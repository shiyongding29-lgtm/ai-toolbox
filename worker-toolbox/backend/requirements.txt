# ── Web 框架 ──
fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.0.0
python-dotenv>=1.0.0
python-multipart>=0.0.9

# ── LLM SDK ──
openai>=1.0.0
anthropic>=0.40.0

# ── 数据库 / ORM ──
sqlalchemy>=2.0.0

# ── 本地模型推理（意图分类 / 多工具检测 / 情感分析）──
torch>=2.1.0
transformers>=4.36.0
# 训练脚本用（仅重新训练模型时需要）
scikit-learn>=1.3.0

# ── 数据分析 / 可视化 ──
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0

# ── 文档 / 文件处理 ──
PyMuPDF>=1.23.0
python-pptx>=0.6.23
Pillow>=10.0.0

# ── 音频 / 转写 ──
faster-whisper>=1.0.0

# ── 网络 / 抓取 ──
httpx>=0.27.0
beautifulsoup4>=4.12.0

# ── 定时任务 ──
APScheduler>=3.10.0
